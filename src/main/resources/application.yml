server:
  port: 8080
  context-path: /
spring:
  application:
    name: service-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
security:
  basic:
    enabled: false